import { Text, View } from 'react-native';
import styles from '../style/style';

export default MainPage = ({ navigation }) => {
    return (
        <View>
            
            <Text>My car</Text>
            <Text>Testi</Text>
        </View>

    );
}